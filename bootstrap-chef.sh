# Replica Labs Setup with Chef #

Make sure your virtual machine is set to at least 2 GB (or higher) memory allocated, otherwise the setup will fail.  


## Clone this repository into the chef-repo folder ##
```
git clone [https url] chef-repo
```

## Run setup.sh ##
```
cd chef-repo
sudo bash setup.sh
```